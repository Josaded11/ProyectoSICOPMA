<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cc.josaded.proyectosicopma">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity"/>
        <activity android:name=".PasosActivity" />
        <activity android:name=".DerivadaActivity" />
        <activity android:name=".EcuacionActivity" />
        <activity android:name=".PasosDerivadaActivity" />
        <activity android:name=".FormularioActivity" />
        <activity android:name=".IntegralActivity" />
        <activity android:name=".HistorialActivity" />
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>